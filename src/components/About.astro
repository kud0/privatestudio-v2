---
// About Section - AMIRO Aesthetic
---

<section class="about-section" id="about">
  <div class="about-container">
    <div class="about-content">
      <div class="about-text">
        <h2 class="about-title">About PrivateStudio</h2>
        <p class="about-subtitle">Tu barbería en Barcelona</p>

        <div class="about-body">
          <p>
            En el corazón de Barcelona, <strong>PrivateStudio</strong> es más que una barbería.
            Es un espacio donde la tradición se encuentra con la modernidad,
            donde cada corte es una obra de arte.
          </p>

          <p>
            Nuestro equipo de profesionales está dedicado a brindarte
            la mejor experiencia en cuidado personal masculino. Utilizamos
            productos premium de <strong>STMNT</strong> y técnicas de vanguardia
            para garantizar resultados excepcionales.
          </p>

          <p>
            Desde cortes clásicos hasta los estilos más modernos, en PrivateStudio
            transformamos tu look con precisión y estilo. Agenda tu cita y descubre
            por qué somos la barbería de confianza en Barcelona.
          </p>
        </div>

        <div class="about-stats">
          <div class="stat-item">
            <span class="stat-number">10+</span>
            <span class="stat-label">Años de experiencia</span>
          </div>
          <div class="stat-item">
            <span class="stat-number">5000+</span>
            <span class="stat-label">Clientes satisfechos</span>
          </div>
          <div class="stat-item">
            <span class="stat-number">4</span>
            <span class="stat-label">Expertos profesionales</span>
          </div>
        </div>
      </div>

      <div class="about-image">
        <div class="image-frame">
          <img
            src="/images/placeholders/about-background.jpg"
            alt="PrivateStudio Interior"
            class="about-img"
            loading="lazy"
          />
        </div>
      </div>
    </div>
  </div>
</section>

<script>
  import { gsap } from 'gsap';
  import { ScrollTrigger } from 'gsap/ScrollTrigger';

  gsap.registerPlugin(ScrollTrigger);

  // Fade in elements on scroll
  gsap.from('.about-text', {
    opacity: 0,
    y: 40,
    duration: 0.8,
    ease: 'power2.out',
    scrollTrigger: {
      trigger: '.about-section',
      start: 'top 70%',
    }
  });

  gsap.from('.about-image', {
    opacity: 0,
    x: 40,
    duration: 0.8,
    ease: 'power2.out',
    scrollTrigger: {
      trigger: '.about-section',
      start: 'top 70%',
    }
  });

  // Stats counter animation
  const statNumbers = document.querySelectorAll('.stat-number');

  const statsObserver = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        const target = entry.target;
        const finalValue = target.textContent;
        const numericValue = parseInt(finalValue.replace(/\D/g, ''));

        if (numericValue) {
          gsap.from(target, {
            textContent: 0,
            duration: 2,
            ease: 'power1.out',
            snap: { textContent: 1 },
            onUpdate: function() {
              target.textContent = Math.ceil(this.targets()[0].textContent) + (finalValue.includes('+') ? '+' : '');
            }
          });
        }

        statsObserver.unobserve(target);
      }
    });
  }, { threshold: 0.5 });

  statNumbers.forEach(stat => statsObserver.observe(stat));
</script>

<style>
  .about-section {
    position: relative;
    padding: 80px 0;
    background-color: #2F2F2F;
    overflow: hidden;
  }

  .about-container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 24px;
  }

  /* Content Layout */
  .about-content {
    display: grid;
    grid-template-columns: 1fr;
    gap: 48px;
    align-items: center;
  }

  /* Text Content */
  .about-text {
    display: flex;
    flex-direction: column;
    gap: 24px;
  }

  .about-title {
    font-family: "bicyclette", sans-serif;
    font-size: 48px;
    font-weight: 700;
    line-height: 1.2;
    color: #E2E0DF;
    margin: 0;
  }

  .about-subtitle {
    font-family: "Montserrat", sans-serif;
    font-size: 18px;
    color: #968C8C;
    font-weight: 600;
    letter-spacing: 0.05em;
    margin: 0;
  }

  .about-body {
    display: flex;
    flex-direction: column;
    gap: 24px;
    margin-top: 8px;
  }

  .about-body p {
    font-family: "Montserrat", sans-serif;
    font-size: 16px;
    line-height: 1.6;
    color: #d0d0d0;
    margin: 0;
  }

  .about-body strong {
    color: #E2E0DF;
    font-weight: 600;
  }

  /* Stats */
  .about-stats {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(120px, 1fr));
    gap: 32px;
    margin-top: 32px;
    padding: 32px 0;
    border-top: 1px solid #474444;
  }

  .stat-item {
    display: flex;
    flex-direction: column;
    gap: 8px;
  }

  .stat-number {
    font-family: "bicyclette", sans-serif;
    font-size: 42px;
    font-weight: 700;
    color: #968C8C;
    line-height: 1;
  }

  .stat-label {
    font-family: "Montserrat", sans-serif;
    font-size: 13px;
    color: #d0d0d0;
    letter-spacing: 0.05em;
  }

  /* Image */
  .about-image {
    position: relative;
  }

  .image-frame {
    position: relative;
    width: 100%;
    border-radius: 4px;
    overflow: hidden;
    box-shadow: 0 32px 68px rgba(0, 0, 0, 0.3);
  }

  .about-img {
    width: 100%;
    height: auto;
    display: block;
    transition: all 0.4s ease;
  }

  .image-frame:hover .about-img {
    transform: scale(1.05);
  }

  /* Tablet */
  @media (min-width: 768px) {
    .about-section {
      padding: 100px 0;
    }

    .about-content {
      grid-template-columns: 1fr 1fr;
      gap: 64px;
    }

    .about-title {
      font-size: 64px;
    }

    .about-subtitle {
      font-size: 20px;
    }

    .about-body p {
      font-size: 18px;
    }

    .stat-number {
      font-size: 48px;
    }

    .stat-label {
      font-size: 14px;
    }
  }

  /* Desktop */
  @media (min-width: 1024px) {
    .about-section {
      padding: 120px 0;
    }

    .about-title {
      font-size: 72px;
    }

    .about-body p {
      font-size: 20px;
    }

    .about-stats {
      margin-top: 48px;
      padding: 48px 0;
    }
  }

  /* Performance */
  @media (prefers-reduced-motion: reduce) {
    .about-img {
      transition: none;
    }

    * {
      animation-duration: 0.01ms !important;
      animation-iteration-count: 1 !important;
      transition-duration: 0.01ms !important;
    }
  }
</style>
